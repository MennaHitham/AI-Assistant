version: '3.8'

services:
  rag-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./AI-Assistant:/app/AI-Assistant
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    command: python AI-Assistant/manage.py runserver 0.0.0.0:8000
